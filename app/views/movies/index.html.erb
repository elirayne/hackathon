<h1>Browse Movies</h1>

<%= link_to 'Create a new movie', new_movie_path %>
<ul>
  <% @movies.each do |movie| %>
    <li>
      <%= link_to movie.title, movie_path(movie) %>
    </li>
  <% end %>
</ul>
