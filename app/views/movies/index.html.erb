<h1>Movies#index</h1>

<%= link_to 'Create a new move', new_movie_path %>
<ul>
  <% @movies.each do |movie| %>
    <li>
      <%= link_to movie.title, movie_path(movie) %>
    </li>
  <% end %>
</ul>
